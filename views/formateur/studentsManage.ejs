<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css">
    <title>Add Student</title>
    <style>
        .container {
            width: 100%;
            max-width: 100vw;
            justify-content: center;
            padding: 34px;
        }

        h2 {
            font-family: sans-serif;
            text-align: center;
            font-size: 24px;
            color: #003b04;
            margin-bottom: 20px;
        }

        form {
            display: flex;
            flex-direction: column;
            margin-left: 34px;
            width: 250px;
        }

        .inputs div {
            margin-bottom: 15px;
        }

        .inputs input {
            padding: 10px;
            font-size: 14px;
            border: 1px solid #88daab;
            border-radius: 5px;
            width: 100%;
            box-sizing: border-box;
            transition: border 0.3s ease;
        }

        .inputs input:focus {
            border-color: #4CAF50;
            outline: none;
        }

        button {
            padding: 12px 20px;
            background-color: #4CAF50;
            color: #fff;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            margin-top: 15px;
        }

        button:hover {
            background-color: #155f09;
        }

        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }
        }
    </style>
</head>
<body>
<div class="container">
    <h2>Add a New Student</h2>

    <form id="addStudentForm" method="post" action="student/createStudent">
        <div class="inputs">

            <div>
                <input type="text" id="studentName" name="first_name" placeholder="First Name" required>
            </div>

            <div>
                <input type="text" id="studentLastName" name="last_name" placeholder="Last Name" required>
            </div>

            <div>
                <input type="email" id="studentEmail" name="email" placeholder="Email" required>
            </div>

            <div>
                <input type="password" id="studentPassword" name="password" placeholder="Password" required>
            </div>

            <div>
                <input type="date" id="studentBirthDate" name="birth_date" required>
            </div>

            <div>
                <input type="text" id="studentAddress" name="address" placeholder="Address" required>
            </div>

            <div>
                <input type="text" id="studentCourse" name="course" placeholder="Course of Study" required>
            </div>

            <div>
                <input type="number" id="studentYear" name="enrollment_year" placeholder="Year" min="1900" max="2099" required>
            </div>

        </div>
        <button type="submit">Add Student</button>
    </form>
</div>

<script src="/js/student.js"></script>
</body>
</html>
